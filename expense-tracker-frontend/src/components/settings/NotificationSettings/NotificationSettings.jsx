/**
 * Placeholder für NotificationSettings Komponente
 */

const NotificationSettings = () => {
  return (
    <section className="notification-settings">
      {/* NotificationSettings content will be added later */}
    </section>
  );
};

export default NotificationSettings;
