/**
 * Placeholder für ThemeSelector Komponente
 */

const ThemeSelector = ({ currentTheme, onThemeChange }) => {
  return (
    <div className="theme-selector">
      {/* ThemeSelector content will be added later */}
    </div>
  );
};

export default ThemeSelector;
