/**
 * Placeholder für CategorySelect Komponente
 */

const CategorySelect = ({ value, onChange }) => {
  return (
    <select className="category-select" value={value} onChange={onChange}>
      {/* CategorySelect content will be added later */}
    </select>
  );
};

export default CategorySelect;
