/**
 * Placeholder für DateRangePicker Komponente
 */

const DateRangePicker = ({ startDate, endDate, onChange }) => {
  return (
    <div className="date-range-picker">
      {/* DateRangePicker content will be added later */}
    </div>
  );
};

export default DateRangePicker;
