╔══════════════════════════════════════════════════════════════════════════╗
║                   🛠️  ADMIN USER MANAGEMENT CHEAT SHEET                  ║
╚══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│ 📊 ÜBERSICHT & STATISTIKEN                                              │
└─────────────────────────────────────────────────────────────────────────┘

  npm run admin:stats                → Statistiken anzeigen
  npm run admin:list                 → Alle Users (max 50)
  node admin-cli.js list --search X  → User suchen
  node admin-cli.js list --verified  → Nur verifizierte

┌─────────────────────────────────────────────────────────────────────────┐
│ 🔍 USER DETAILS                                                          │
└─────────────────────────────────────────────────────────────────────────┘

  node admin-cli.js get <userId>     → Details + Transaktionen

┌─────────────────────────────────────────────────────────────────────────┐
│ 🔑 PASSWORT MANAGEMENT                                                   │
└─────────────────────────────────────────────────────────────────────────┘

  node admin-cli.js reset-password <userId> test123
                                     → Passwort auf "test123" setzen

┌─────────────────────────────────────────────────────────────────────────┐
│ 🗑️  USER LÖSCHEN                                                         │
└─────────────────────────────────────────────────────────────────────────┘

  node admin-cli.js delete <userId>  → User + Transaktionen löschen
  node admin-cli.js clean-all        → ALLE Users löschen (⚠️)

┌─────────────────────────────────────────────────────────────────────────┐
│ 🌐 REST API (Browser/Postman/cURL)                                      │
└─────────────────────────────────────────────────────────────────────────┘

  http://localhost:3000/api/admin/stats
  http://localhost:3000/api/admin/users
  http://localhost:3000/api/admin/users?search=max
  http://localhost:3000/api/admin/users/<userId>

  PATCH /api/admin/users/<userId>
  Body: {"isVerified": true}

  POST /api/admin/users/<userId>/reset-password
  Body: {"newPassword": "test123"}

  DELETE /api/admin/users/<userId>

┌─────────────────────────────────────────────────────────────────────────┐
│ 🧪 WORKFLOW: Testuser erstellen                                         │
└─────────────────────────────────────────────────────────────────────────┘

  1. POST /api/auth/register
     {"name": "testuser", "password": "Test123!", "email": "test@x.de"}

  2. node admin-cli.js list --search testuser
     → Kopiere User-ID

  3. node admin-cli.js reset-password <userId> test123

  4. curl -X PATCH http://localhost:3000/api/admin/users/<userId> \
       -H "Content-Type: application/json" \
       -d '{"isVerified": true}'

  5. Login: testuser / test123

┌─────────────────────────────────────────────────────────────────────────┐
│ 💾 MONGODB DIREKT (mongosh)                                             │
└─────────────────────────────────────────────────────────────────────────┘

  mongosh "mongodb://localhost:27017/finora-dev"

  db.users.find().pretty()            → Alle Users
  db.users.countDocuments()           → Anzahl
  db.users.find({name: /max/i})       → Suchen
  db.users.deleteMany({})             → ALLE löschen

┌─────────────────────────────────────────────────────────────────────────┐
│ 📁 WICHTIGE DATEIEN                                                      │
└─────────────────────────────────────────────────────────────────────────┘

  SOFORT_STARTEN.md         → Schnellstart-Guide
  docs/ADMIN_API.md         → Vollständige Doku
  admin-api.http            → REST-Beispiele (VS Code REST Client)
  admin-cli.js              → CLI Tool Source

┌─────────────────────────────────────────────────────────────────────────┐
│ 🆘 TROUBLESHOOTING                                                       │
└─────────────────────────────────────────────────────────────────────────┘

  ❌ ECONNREFUSED           → Server läuft nicht: npm run dev
  ❌ "only in development"  → .env: NODE_ENV=development
  ❌ "User nicht gefunden"  → Falsche ID: npm run admin:list
  ❌ CLI zeigt nichts       → Keine Users: Erst registrieren!

┌─────────────────────────────────────────────────────────────────────────┐
│ ⚡ NPM SHORTCUTS                                                         │
└─────────────────────────────────────────────────────────────────────────┘

  npm run admin              → Hilfe anzeigen
  npm run admin:stats        → Statistiken
  npm run admin:list         → User-Liste

╔══════════════════════════════════════════════════════════════════════════╗
║  💡 TIPP: Server muss laufen! → npm run dev (in anderem Terminal)       ║
╚══════════════════════════════════════════════════════════════════════════╝
